import styled from 'styled-components';

export const ContainerButtonContato = styled.button`
  display: inline-flex;
  align-items: center;
  justify-content: center;
  gap: 8px;
  padding: 10px 16px;
  background: transparent;

  border-radius: 16px;
  border: 1px solid rgba(123, 74, 226, 0.5);

  > p {
    font-size: 16px;
    font-weight: 700;
    color: rgba(123, 74, 226, 0.5);
  }
`;
