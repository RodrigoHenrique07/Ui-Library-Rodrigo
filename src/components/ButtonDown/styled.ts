import styled from 'styled-components';

export const ContainerButtonDown = styled.button`
  display: inline-flex;
  gap: 4px;
  align-items: center;
  justify-content: center;
  background: transparent;
  border: none;

  > p {
    font-size: 16px;
    font-weight: 700;
    color: #7b4ae2;
  }


`;
